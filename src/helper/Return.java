package helper;
/**
 * Enumerated Types used for parsing user input and determining what they wanted to do
 * @author Steve Flynn
 *
 */
public enum Return {
	
	/**
	 * These types are returned based on what the player wanted to do
	 */
	SUCCESS, ERROR, RESIGN, DRAW1, DRAW2, PROMO;
}
